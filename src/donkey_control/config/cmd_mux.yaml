command_mux:
  ros__parameters:
    output_topic: /drive_cmd
    publish_rate_hz: 20.0

    input_names:
      - estop
      - teleop
      - autonomy

    input_topics:
      - /cmd/estop
      - /cmd/teleop
      - /cmd/autonomy

    priorities:
      - 100
      - 50
      - 10

    timeouts:
      - 0.25
      - 0.50
      - 0.50